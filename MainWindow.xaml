<Window x:Class="WPFValidacion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFValidacion"
        mc:Ignorable="d"
        Title="Validación de Campos" Height="400" Width="800">
    <Grid>
        <Label Content="Información de Usuario" HorizontalAlignment="Left" Margin="259,51,0,0" VerticalAlignment="Top" FontSize="20" FontFamily="Calisto MT"/>
        <StackPanel Margin="10">
            <TextBox Name="nombreTextBox" Margin="5" Width="200" VerticalAlignment="Center">
                <TextBox.Text>
                    <Binding Path="Nombre" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:NombreValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Button Content="Validar" Click="ValidateButton_Click" Margin="5" Width="200"/>
        </StackPanel>
    </Grid>
</Window>
